#host_os = "windows"
#host_os = "linux"
# use linix if on macos

# comment this out as part of "separate contexts for variables project"
# host_os is no longer referenced
# NOTE: that the .tpl files are built on the host_os, i.e. linux-ssh-config.tpl and
# windows-ssh-config.tpl.